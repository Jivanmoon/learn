5.1
    空语句就是什么只包含一个单独分号的语句；空语句用于语法上需要一条语句但是逻辑上不需要的地方

5.2
    块是指用花括号括起来的语句和声明的序列；块用于在程序中语法上需要一条语句，但是逻辑上需要多条语句的地方

5.3
    code

5.4
    (a)这个循环试图用迭代器遍历string，但是变量的定义应该放在循环的外面，目前每次循环都会重新定义一个变量，明显是错误的。
    (b)这个循环的 while 和 if 是两个独立的语句，if 语句中无法访问 status 变量，正确的做法是应该将status变量定义在while循环外

5.5
    code

5.6
    code

5.7
    (a)ival1 = ival2;
    (b)添加{}
    (c)if(!ival)应该改为else
    (d)ival == 0

5.8
    多个if语句嵌套时，如何知道某个else是与哪个if语句匹配的问题称作悬垂else
    else字句与最近的未适配的if语句适配

5.9~5.12
    code

5.13
    (a)每条case后面没有加break，而且对iou的判断不应该写到default中
    (b)不应该在default中使用前面的ix变量，因为可能导致ix变量没有初始化就直接使用
    (c)case后面不能跟多个标签，应该分开来写，偶数判断应该是0,2,4,6,8
    (d)case标签必须是整型常量表达式

5.14
    code

5.15
    (a)for循环中定义的变量只能在for循环语句块中使用，ix变量在外部无定义
    (b)应该在ix != sz之前加;
    (c)++sz之后，ix != sz的判断条件发生变化，则改for循环会成为死循环

5.16
    while循环的通用性更好一些

5.17
    code

5.18
    (a)do后面的语句块必须使用花括号包起来
    (b)do while里面的condition使用的变量必须定义在循环体之外
    (c)do while里面的condition使用的变量必须定义在循环体之外

5.19
    code