(a)非法定义，不能直接使用未定义的变量
    改正先定义变量，后赋值
(b)非法定义，因为3.14字面值为double类型，使用列表初始化时，因为要初始化的变量i为int类型，会有可能造成丢失数据，编译器会报错
    改正将int转换为double
(c)非法定义，在这里wage属于未定义变量
    改正先定义salary并初始化，然后定义wage，此时可用salary对wage进行初始化
(d)合法定义，但会造成信息丢失